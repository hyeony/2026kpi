{
  "name": "test1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm run build:core && pnpm run build:theme && pnpm --filter playground dev",
    "build": "pnpm run build:core && pnpm run build:theme && pnpm --filter playground build",
    "build:core": "pnpm --filter @test1/core build",
    "build:theme": "pnpm --filter @test1/theme build",
    "lint": "pnpm -r run lint"
  }
}
